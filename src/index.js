import fecha from 'fecha'
import math from 'date-arithmetic'
import {DateRangeCreate, DateRange} from 'date-range'


export default {
    math: math,
    format: fecha,
    range: DateRange,
    rangeCreate: DateRangeCreate
}